% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/product.R
\name{rsyc_product}
\alias{rsyc_product}
\title{Combine RSYC Yield Curves with a Boundary Map}
\usage{
rsyc_product(
  response,
  species,
  age = 1:150,
  strata_level = "tile",
  strata_id = NULL,
  output = NULL,
  overwrite = FALSE,
  cache_dir = tools::R_user_dir("RSYC", "data"),
  quiet = TRUE
)
}
\arguments{
\item{response}{Forest measure to estimate: \code{"agb"} for aboveground biomass
or \code{"volume"} for total volume.}

\item{species}{One or more species codes or broad groups. Uppercase and
lowercase letters are treated the same.}

\item{age}{One or more stand ages in years.}

\item{strata_level}{Type of area: \code{"tile"}, \code{"ecozone"},
\code{"ecoprovince"}, \code{"ecoregion"}, or \code{"ecodistrict"}.}

\item{strata_id}{One or more tile IDs, ecological area names, or full
sequences of ecological area names. By default, every area with a matching
published curve is included. If an area name occurs in several places,
the function asks for the full sequence of area names.}

\item{output}{Location of an optional GeoPackage file. When supplied, the
file contains a boundary map named \code{spatial}, a yield table named \code{curves},
and a summary table named \code{metadata}.}

\item{overwrite}{Replace an existing GeoPackage at \code{output}?}

\item{cache_dir}{Folder in which to save the GeoPackage. By default, RSYC
uses its standard user data folder, returned by \code{\link[tools:userdir]{tools::R_user_dir()}}.}

\item{quiet}{Hide download progress?}
}
\value{
Three named results: \code{spatial}, an \code{sf} boundary map; \code{curves}, a
table of yield estimates; and \code{metadata}, a one-row summary of the data and
choices used.
}
\description{
Estimate yield for one or more species across a set of tiles or ecological
areas. The boundary map is kept separate from the curve table so that each
area shape is stored only once.
}
\details{
The function currently uses only curves fitted across Canada. It does not
substitute a curve from another type of area or from a regional fit.
Ecological boundaries must first be downloaded with
\code{\link[=download_rsyc_boundaries]{download_rsyc_boundaries()}}; tile boundaries are included with RSYC.
}
\examples{
tile_product <- rsyc_product(
  response = "agb",
  species = c("PICE.MAR", "POPU.TRE"),
  age = c(50, 100, 150),
  strata_level = "tile",
  strata_id = c("H14", "F31")
)
tile_product$spatial
tile_product$curves

}
