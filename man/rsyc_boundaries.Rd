% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecostrat.R
\name{rsyc_boundaries}
\alias{rsyc_boundaries}
\title{Read RSYC Boundary Maps}
\usage{
rsyc_boundaries(
  strata_level = "tile",
  cache_dir = tools::R_user_dir("RSYC", "data"),
  quiet = TRUE
)
}
\arguments{
\item{strata_level}{Type of area: \code{"tile"}, \code{"ecozone"}, \code{"ecoprovince"},
\code{"ecoregion"}, or \code{"ecodistrict"}.}

\item{cache_dir}{Folder in which to save the GeoPackage. By default, RSYC
uses its standard user data folder, returned by \code{\link[tools:userdir]{tools::R_user_dir()}}.}

\item{quiet}{Hide messages produced while the map is read?}
}
\value{
An \code{sf} map in the Canada Atlas Lambert projection (EPSG:3978). Every
map contains \code{strata_id}, which links an area to its RSYC curves, and the
area shape. Ecological maps also contain the names of the larger ecological
areas in which each area lies.
}
\description{
Read the tile boundaries or ecological boundaries used for the RSYC yield
curves. Tile boundaries are included with the package. Ecological boundaries
are read from the file saved by \code{\link[=download_rsyc_boundaries]{download_rsyc_boundaries()}}. This function
never downloads data on its own.
}
\examples{
tiles <- rsyc_boundaries("tile")
\dontrun{
download_rsyc_boundaries()
districts <- rsyc_boundaries("ecodistrict")
}

}
